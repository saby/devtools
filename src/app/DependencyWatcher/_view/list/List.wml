<DependencyWatcher._view.list.DataContainer
    Source="{{ __source }}"
    sourceConfig="{{ _options.sourceConfig }}"
    navigation="{{ __navigation }}"
    column="{{ __column }}"
    grouping="{{ __grouping }}"
    itemTemplate="{{ __itemTemplate }}"
    modeController="{{ _options.modeController }}"
    name="dataContainer"
/>
